{
  "name": "portfolio-homepage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:watch": "sass -w src/scss/main.scss dist/main.css",
    "sass:build": "sass src/scss/main.scss dist/main.css"
  },
  "dependencies": {
    "sass": "^1.79.3"
  }
}
